// PlatformSelector se mantiene oculto — la plataforma ya vive en StrategicInputCard
export default function PlatformSelector(_props: { selectedPlatform: string; onPlatformChange: (p: string) => void }) {
  return null;
}
